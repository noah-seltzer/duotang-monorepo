// Example `tailwind.config.js` file
const colors = require('tailwindcss/colors')

module.exports = {
    theme: {
        colors: {
            emerald: colors.emerald
        },
        fontFamily: {},
        extend: {}
    },
    variants: {}
}
